# iter-snmpcheck
```
usage: iter-snmpcheck.py [-h] [-f <FILE>]

Check a list of IPs using the snmp-check tool to gather additional information
about a host

optional arguments:
  -h, --help  show this help message and exit
  -f <FILE>   file containing a list of IP Addresses (1 IP per line)
```
